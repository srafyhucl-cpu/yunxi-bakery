# 待办优先级清单（2026-08-31 校准）

> **排序依据**：先判断是否属于 Harness P0 控制面或阻塞 P2 内部受控试运行的真人执行段，
> 其次是「是否上线前必须」，最后是「是否为增强/可延后」。
>
> **来源标注**：`[交接]` = 来自交接文档（OpenCode 会话转录）；`[本地]` = 来自本地仓库文档；
> `[实测]` = 本次评估当场执行命令所得。未标注来源者不作断言。

**as_of**：2026-08-31 ｜ **当前版本**：`0.133.0-p2trial.3` ｜ **最新提交**：`b4f25db`

> 版本号以 `backend/VERSION` 为准，提交号以当前 `git rev-parse HEAD` 为准。本清单只维护活动队列和依赖，不覆盖 [PROJECT-STATE.md](../PROJECT-STATE.md) 的阶段状态。
>
> **2026-08-31 优先级调整**：中文治理属于 Harness 的项目管理与驾驭工程能力，已并入 P0 控制面；不另设“翻译项目”。P0 中文治理只覆盖权威状态、责任、阻塞、证据、交接和高风险用户可见路径；全仓注释翻译、低风险文案和历史文档清理仍按 P1/P2 排期。

---

## 判据说明

| 级别 | 定义 | 时间要求 |
|------|------|---------|
| **P0** | Harness 控制面缺口，或阻塞 P2 真人试运行/高风险结论 | 立即，作为执行与发布前置 |
| **P1** | 持续评测、观测、回放和协作增强（不直接阻塞启动，但影响治理完整性） | P2 启动前或本迭代 |
| **P2** | 正式上线前必须完成 | 2027-06 前 |
| **P3** | 增强项 / 可延后 / 独立轨道 | 无强制期限 |

---

## P0 —— Harness 控制面与 P2 真人执行阻塞

### Harness P0 控制面（新增）

| 编号 | 待办 | 状态 | 依据 |
|---|------|------|------|
| H1 | `T-HARNESS-CHINESE-GOVERNANCE`：中文治理控制面 | ✅ 已完成（completed） | [本地] `PROJECT-STATE.md`；`E-20260831-001`；本轮 trace `20260831-p0-execution` |
| H2 | `T-HARNESS-RUN-MANIFEST`：统一运行 manifest 与 episode 回放 | ✅ 已完成（completed） | [本地] `PROJECT-STATE.md`；`E-20260831-001`；本轮 trace `20260831-p0-execution` |
| H3 | `T-HARNESS-POLICY-AS-CODE`：工具权限与生产边界策略即代码 | ✅ 已完成（completed） | [本地] `PROJECT-STATE.md`；`E-20260831-001`；本轮 trace `20260831-p0-execution` |
| H4 | `T-HARNESS-CI-ENTRY`：仓库级 Harness CI 统一入口 | ✅ 已完成（completed） | [本地] `PROJECT-STATE.md`；`E-20260831-001`；本轮 trace `20260831-p0-execution` |
| H5 | `T-HARNESS-CLEANUP-POLICY`：临时与可重建产物递归清理规则收口 | ✅ 已完成（completed） | [本地] `PROJECT-STATE.md`；`E-20260831-002`；本轮 trace `20260831-cleanup-policy` |

这四项是 Harness P0 的共同前置：在 P2 真人 B/C/D/E 执行或任何高风险“完成/通过/已上线”结论前，必须具备中文状态说明、责任人、策略快照、运行摘要、证据路径和未验证范围。它们可以与负责人审批和测试号准备并行推进，但不能被 P1/P2 低风险文案工作替代。

### 业务 P0-1. [已完成] 修复 `check:miniapp` 守卫并收口 AI 聊天超时改动

**[实测 / 已收口]** commit `77f9346` 已完成守卫兼容与 AI 聊天超时覆盖；该项不再是当前 P0 阻塞。

- `transport.ts` 支持 `options.timeoutMs ?? REQUEST_TIMEOUT_MS`，保留默认超时兜底。
- `services/chat.ts` 将 AI 发消息超时单独设为 60000ms，其余接口仍为 12000ms。
- 守卫回归断言覆盖固定值、覆盖+兜底、无 timeout、无默认兜底和错误键名五种情况。
- `npm run typecheck` → EXIT=0；`npm run check:miniapp` → EXIT=0（15 pages / 15 routes）。

**剩余事项**：C1–C5 仍需按 P2 手册在 DevTools/真机现场验证；静态守卫通过不等于现场验收完成。

---

### 业务 P0-2. 项目负责人确认 P1 关闭并启动 P2 真人执行段 🔴

**[本地]** `PROJECT-STATE.md` L92 / L28

- 性质：**纯决策，零技术阻塞**，是全部后续工作的前置闸门
- 红线原文：*「未获项目负责人确认前，不得擅自启动 P2 实测」*
- 卡点：等**人**，不等代码

> 提醒：这一项无法由 AI 代为推进，需你本人拍板。

---

### 业务 P0-3. 解决体验版上传 —— 须拆成两个独立问题 🔴

**[交接] + [本地]** 这两个问题此前被混为一谈，必须分开处理：

| 子项 | 性质 | 解法 | 来源 |
|------|------|------|------|
| 3a. DevTools `Empty file` 报错 | **瞬时竞态，非代码缺陷** | 点「编译」Ctrl+B 恢复；仍报错则关窗口重开 | **[交接]** 09:52:27 读取撞上 DevTools 重写，09:52:32 落盘 |
| 3b. 测试号不支持体验版 | **硬阻塞** | 需 mp 后台条件 | **[本地]** `PROJECT-STATE.md` L113 |

3a 已随交接文档查明、可立即消除；**3b 仍然存在**，是真人试运行的真实路径阻塞。

---

## P1 —— P2 试运行前必须完成

| # | 待办 | 状态 | 依据 |
|---|------|------|------|
| 4 | 知识缺口回填（5 条 production 缺口） | ⏸ 降级不阻塞，转人工兜底已验证。<br>⚠️ **仓库内没有这 5 条的清单**，必须先枚举再回填 | **[本地]** PROJECT-STATE L88；表定义在 `v004_agent_foundation_tables.sql:40` |
| 5 | 发票承接 | ⚠️ 代码已在 `e2639d4` 落地，剩余专用测试与端到端验收 | **[实测]** API 3 端点 + v027 迁移 + repo + service + admin 前端 dist 均已存在；`backend/tests/` 下无 invoice 测试 |
| 6 | P2 模拟器 B / C / D / E 项走查 | ⏸ A 项已完成，其余待排 | **[本地]** PROJECT-STATE L112 |
| 7 | FAQ 模板 10 条的店家填写结果回收 | ❌ **已被项目负责人否决，不进入当前待办**；原草案仅作历史参考 | **[决策]** 项目负责人明确否决；`docs/tasks/20260829-P1-7-FAQ回收-指令.md` 仅保留历史记录 |

### P1 各项的 AI 执行指令

| # | 指令文件 | 说明 |
|---|---------|------|
| 4 | `docs/tasks/20260829-P1-4-知识缺口回填-指令.md` | 首步是枚举，不是回填 |
| 5 | `docs/tasks/20260829-P1-5-发票承接验收-指令.md` | 补测试 + 验收，不改业务代码 |
| 6 | `docs/tasks/20260829-P1-6-模拟器BCDE走查-指令.md` | 走查记录，发现缺陷只登记不修 |
| 7 | `docs/tasks/20260829-P1-7-FAQ回收-指令.md` | 历史否决事项，禁止执行 |

---

## P2 —— 正式上线前必须

| # | 待办 | 风险 | 依据 |
|---|------|------|------|
| 8 | `shop_config.featured_products` 精选 6 款补 DB 迁移 / 发布配置 | **生产上线会出现空白货架**；当前仅本地库直写，无迁移覆盖 | **[本地]** PROJECT-STATE L114 原文标注「上线前必须」 |
| 9 | 微信支付商户号申请跟进 | 商户号未下，支付全 mock | **[本地]** PROJECT-STATE L63 |
| 10 | 有赞 IP 白名单：确认 NAT 后真实出口 IP | `10.x` 为内网段，同步链路可能不稳 | **[本地]** PROJECT-STATE L62（已提出未闭环） |
| 11 | 处置过期副本 `D:\Project\YunxiBakeMiniApp` | 误用会导致基于错误代码库开发/上传 | **[交接]** 8/14 冻结，`45d5235`，缺 `utils/time-format.ts` |
| 12 | 里程碑计划重新基线化 | 计划书 5.1 时间线已被 v1.2/v1.3/v1.4 三次修正绕开且未回写，已失去对照价值 | **[本地]** 本次评估发现 |
| 13 | 生产部署状态复核 | 最新证据停留在 2026-08-10（v`0.109.16` / `51d315748b`） | **[本地]** 进度清单 08-10 条目 |

> **关于第 11 项**：建议归档或加明显标记，**不要删除** —— 删除需你明确指示。

---

## P3 —— 可延后 / 独立轨道

| # | 待办 | 说明 | 依据 |
|---|------|------|------|
| 14 | 商品搜索入口 | 309 SKU 无搜索，后端参数现成 | PROJECT-STATE L90（P3 增强） |
| 15 | FP-5 技术债（类型注解 + `tests/scripts` WinError 32 flaky） | 纯类型与测试工程，无业务行为变更 | `docs/specs/2026-08-12-member-loyalty-followup-techdebt.md` |
| 16 | D1 账务核心 | **冻结**，独立轨道；D1-A 不得合入 master，D1-B 不得放行 | PROJECT-STATE L48；进度清单 D1-A.2.1 段 |
| 17 | Windows 测试 `skipif(win32)` 4 项 | backup×3 的 C 盘守卫 + `test_cli_help` gbk 解码 | PROJECT-STATE L119-120 |
| 18 | 模拟器 Console Error: timeout 未定位 | Launch Time 偏慢，非阻断 | PROJECT-STATE L115 |

---

## 依赖关系

```
[2 项目负责人确认]  ← 闸门，全部前置
        │
        ├── [1 守卫与超时改动]       → ✅ 已完成；C 项仍需现场验证
        ├── [3a 重编译消除竞态]      → 已可立即执行
        └── [3b 测试号/体验版]       → 硬阻塞，需 mp 后台
                    │
                    ▼
        [P2 真人执行段 A–E]
                    │
                    ▼
        [4-7 P1 项] → [8-13 P2 上线前项] → 2027-06 最早候选窗口（须批准）
```

---

## 建议的下一步（按执行顺序）

1. **在 DevTools 点「编译」** 消除 3a 瞬时竞态报错（1 分钟）。
2. **你去拍板第 2 项**：确认 P1 关闭、启动 P2 真人执行段。
3. 同步确认第 3b 项测试号/AppID 体验版路径是否已具备条件。
4. 按任务指令推进 4–7 项，并分别记录实际退出码或现场证据。

守卫改动已由 `77f9346` 收口；当前待负责人确认的是 P1 阶段关闭和 P2 真人执行段启动。
