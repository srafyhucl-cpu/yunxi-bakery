# P1-6 任务指令：P2 模拟器 B / C / D / E 项走查

> owner: AI 员工 + 项目负责人（现场授权与记录）
> status: blocked
> status_label: 已阻塞（blocked）
> task_id: T-P1-6-SIMULATOR
> workstream: P2（任务优先级为 P1，执行内容为 P2 真人执行前走查）
> as_of_commit: b4f25db
> version: 0.133.0-p2trial.3
> branch: main
> allowed_paths: `miniapp/reports/**`、`docs/harness-engineering/core/evidence-index.md`（证据登记）、本任务文件、`LOGBOOK.md`（owner 收口）
> forbidden_paths: `backend/**`、`miniapp/miniprogram/**` 业务代码、`miniapp/project.config.json`、正式 AppID/发布配置、旧仓 `D:\Project\YunxiBakeMiniApp`
> source_of_truth: PROJECT-STATE.md

> **用法**：复制下方代码块内的全部内容，粘贴到新的 AI 会话中执行。
> **编写时间**：2026-08-29 ｜ **预计耗时**：2–3 小时
> **前置**：P0-1（AI 聊天超时守卫）已完成并推送，C 项的静态超时技术阻塞已解除；这不代表体验版测试号限制或 C1–C5 现场验收已完成。

---

```
你正在处理「芸熙烘焙智能经营平台」项目。

仓库路径：D:\Project\YunxiBakery（Monorepo，含 backend/ 与 miniapp/）
当前版本：0.133.0-p2trial.3
最新提交：b4f25db

## 任务

trace_id: 20260829-p2-simulator-bcde
parent_trace_id: 20260826-p2trial-sim-fixes

在微信开发者工具模拟器中执行 P2 内部受控试运行手册的 B / C / D / E 四项走查。

## 背景

依据 docs/specs/2026-08-25-p2-manual.md，P2 试运行共 5 项（A–E）：

- **A 项（商品浏览 / 搜索 / 分类兜底）已完成**（2026-08-26）
- B / C / D / E **待走查**（PROJECT-STATE.md 第 112 行）

本次走查范围即 B、C、D、E 四项。

## ⚠️ 三条必须先知道的预期行为（不要把预期当 bug）

1. **D 项「非会员身份打开积分/券页面显示 loadFailed」属于预期**，不是缺陷。
   会员资产需绑定会员档案（customer_identity_links），普通微信身份未绑定时
   前端正确降级。手册第 69 行已明确说明。
2. **支付全部是 mock**，商户号未下来。若出现任何"要求真实付款"的界面，
   **立即停止并报告**（红线）。
3. **AI 客服响应慢是正常的**：LLM 冷启动可达 48 秒。P0-1 已把超时放宽到 60 秒
   （commit 77f9346），若仍超时请如实记录并报告。

## 走查内容（逐项执行并记录）

### B. 购物车 + 下单 + mock 支付 + 订单列表（优先级 1）

小额原则：指定商品 **黄天鹅水煮蛋 ¥2.00（item_id=5606595692）**
或 **鲜奶提子面包 ¥7.00（item_id=5954296463）**；每人最多 2 单，累计 ≤ ￥100。

| 步骤 | 操作 | 预期 |
|------|------|------|
| B1 | 商品详情点「加入购物车」 | 提示成功；购物车角标/数量 +1 |
| B2 | 进购物车，核对商品与金额 | 数量可增减；合计金额正确（¥2 或 ¥7 档） |
| B3 | 「去结算」→ 填写联系人/电话、选自提、选预约时间 | 表单校验通过 |
| B4 | 提交订单 | 创建成功，跳转支付；**支付为 mock** |
| B5 | 完成后进「我的-订单」 | 订单出现，状态正确（待付款→已付款流转） |

### C. AI 客服问答（优先级 2）

入口：底部 tab「客服」。逐条发送：

| # | 问题 | 预期 |
|---|------|------|
| C1 | 今天营业时间是几点？ | 正确回答营业时间（09:00-20:00 相关话术） |
| C2 | 订蛋糕怎么订？ | 给出订购流程说明 |
| C3 | 能开发票吗？ | 增值税电子普通发票口径（抬头/税号/邮箱，1-3 个工作日）+ 引导转人工 |
| C4 | 蛋糕坏了怎么办？ | 售后规则 + **必须触发转人工**（留一条转人工记录） |
| C5 | 提子面包甜不甜？多大尺寸？ | 商品维度回答或兜底话术 |

### D. 资产页（积分/券/储值）（优先级 2）

| 步骤 | 操作 | 预期 |
|------|------|------|
| D1 | 我的 → 积分明细 | 非会员：loadFailed 空态（**预期**）；已绑定会员档案者显示余额与流水 |
| D2 | 我的 → 券中心 | 同上预期 |
| D3 | 我的 → 储值余额页 | 充值档位正常展示（4 档）；mock 充值不入真钱 |

### E. 发票登记闭环（优先级 3）

| 步骤 | 操作 | 预期 |
|------|------|------|
| E1 | 客服页发"我需要开发票"（附抬头/税号/邮箱） | AI 回开票口径 + 触发转人工 |
| E2 | 店员侧确认收到人工会话提示 | 会话标记转人工成功 |
| E3 | 负责人登录 admin 后台 → 发票管理 → 登记该请求 | 登记成功，状态 applied |
| E4 | 后台点「标记已开」 | 状态变 issued，可填备注 |

## 记录要求

每项复制手册第三节的模板填写：

    编号: ___   执行人: ___   时间: ___
    项目: B1 / C4 ...
    结果: 通过 / 失败 / 阻塞 / 超限暂停
    截图区间: [起始描述] ~ [结束描述]（或文件名）
    备注: ___

汇总表每人一行。**结果必须如实，不允许把"阻塞"写成"通过"。**

## 异常处理

- 任何步骤失败：截图 + 标"失败"，**不要反复重试超过 2 次**
- 出现真实收款界面：立即停止、截图上报（红线）
- 服务不可用（health 失败）：先检查后端进程再继续
- 若遇 DevTools 报 `project.config.json: Empty file is NOT a valid json file`：
  这是 DevTools 重写配置时的**瞬时竞态**，点「编译」（Ctrl+B）即可恢复，
  不是代码缺陷；仍报错则关闭项目窗口重开

## 约束（违反即返工）

- **这是走查记录任务，不改业务代码**。发现缺陷只登记，不自行修复
  （P0-1 的教训：修复需另行评估与授权）
- DevTools 必须使用 `D:\Project\YunxiBakery\miniapp`
  （AppID wx4b59baadd9187a2e）；**禁止使用旧副本 D:\Project\YunxiBakeMiniApp**
- 清理测试数据属手册第四节，**执行前需项目负责人批准**，先列影响范围再询问，不要擅自清理
- **不上传正式版、不提交审核、不改 appid**
- 不部署、不开放真实用户访问（截至 2027-05-31 含，小程序仅限开发、调试和测试）

## 提交收口（按 docs/AGENTS/commit-workflow.md 9 步）

- 本克隆只有 `origin` 一个远端，`git push server master` **无对应远端，不要执行**
- LOGBOOK.md 在**文件顶部**追加条目，含 verification 与 newly found defects
- 若本轮无代码改动，用 `SKIP_VERSION_BUMP=1` 提交
- push 用 `git -c credential.helper='!gh auth git-credential' push origin main`
- push 后必须 `git ls-remote origin main` 回读核对 SHA

## 汇报格式

1. B / C / D / E 四项逐条结果表（通过 / 失败 / 阻塞）
2. 新发现缺陷清单（含复现步骤与截图位置），**注明是否已按 P2 手册登记**
3. 是否存在需要项目负责人决策的问题（如小额超限、数据清理）
4. commit SHA（若有）、LOGBOOK trace 是否已登记
5. 遗留风险
```

---

## 附：编写本指令时的核实记录

| 核实项 | 结果 | 核实方式 |
|--------|------|---------|
| A 项完成状态 | 已完成（2026-08-26） | PROJECT-STATE.md L112 |
| B/C/D/E 待走查 | 确认 | PROJECT-STATE.md L112 |
| 小额指定商品与 ID | 黄天鹅水煮蛋 5606595692 / 鲜奶提子面包 5954296463 | P2 手册第 27-29 行 |
| D 项 loadFailed 为预期 | 手册第 69 行明确 | 读手册 |
| C 项静态超时技术阻塞 | commit 77f9346 放宽至 60s；体验版测试号限制仍独立存在 | 已核验 |
