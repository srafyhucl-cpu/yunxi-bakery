# P2 任务指令：文档园艺与低风险中文维护

> owner: AI 员工
> status: completed
> status_label: 已完成（completed）
> task_id: T-HARNESS-DOC-GARDEN
> as_of_commit: b4f25db
> version: 0.133.0-p2trial.3
> branch: main
> allowed_paths: `docs/**`、`backend/scripts/check_doc_garden.py`、`backend/tests/scripts/test_check_doc_garden.py`、`.github/workflows/harness-garden.yml`、`backend/reports/harness/`
> forbidden_paths: `backend/app/**`、`backend/data/**`、`backend/reports/production/**`、`.env*`、`.git/**`、生产环境、审计证据删除
> source_of_truth: PROJECT-STATE.md

## 本轮目标

建立只读文档园艺扫描：检查断链、重复任务入口、过期快照、孤立任务、未登记运行报告和低风险中文覆盖。P2 结果不作为当前高风险执行的前置阻断项。

## 执行命令

```powershell
python -B backend/scripts/check_doc_garden.py --summary --fail-on error --output backend/reports/harness/doc-garden-20260831.json
```

## 完成标准

- 错误级断链或入口漂移明确报告；历史归档文档可降级为 warning。
- 低风险中文覆盖和未登记报告只做整改提示，不删除审计证据。
- 周期 CI 失败不改变 `.github/workflows/harness-p0.yml` 的 P0 结果，不阻断生产、支付和客户数据红线判断。
