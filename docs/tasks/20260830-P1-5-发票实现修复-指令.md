# P1-5 后续任务指令：发票状态与必填校验修复

> owner: 项目负责人＋AI 员工
> status: completed
> status_label: 已完成（completed）
> task_id: T-P1-5-INVOICE-FIX
> as_of_commit: 220a8fe
> version: 0.133.0-p2trial.3
> branch: main
> allowed_paths: `backend/app/api/admin/invoices.py`、`backend/app/service/invoice/**`、`backend/app/repository/invoice_repo.py`、`backend/tests/api/test_admin_invoice_api.py`、本任务文件、`PROJECT-STATE.md`、`LOGBOOK.md`、`ERRORS.md`、`docs/harness-engineering/core/evidence-index.md`
> forbidden_paths: `miniapp/**`、生产环境、真实支付、业务数据、旧仓 `D:\Project\YunxiBakeMiniApp`
> source_of_truth: PROJECT-STATE.md

## 目标

修复 P1-5 专用测试已确认的两个实现缺口，完成后台发票登记的最小状态闭环：

1. `companyTitle`、`taxNo`、`email` 缺失或去空格后为空时，接口返回 400 或 422，不写入空字段记录。
2. 已为 `issued` 的记录再次标记，以及其他非 `applied` 状态标记，接口返回 409。
3. 保持现有登记、列表、首次标记已开票行为不变。

## 本轮执行结果（2026-09-06）

- API 模型已将 `companyTitle`、`taxNo`、`email` 改为必填。
- 服务层已拒绝缺失、非字符串和空白字段；仓储层用 `status = 'applied'` 条件更新并依据 `rowcount` 判断状态转换。
- 已移除 4 个严格 `xfail`；发票专用测试 7 项全部通过。
- E1-E4 真实客服、后台和联动验收尚未执行，继续由 `T-P1-5-INVOICE` 承接。

## 实施边界

- 校验放在请求模型和服务层，错误响应可稳定测试，且不依赖数据库 `DEFAULT ''` 充当业务校验。
- 状态流转以服务层解释；仓储层区分“记录不存在”和“状态不允许更新”。
- 不新增依赖，不改 miniapp，不执行真实支付或生产操作。
- 本轮作为项目负责人已裁定必填字段和 409 语义；不新增依赖、不改 miniapp、不执行真实支付或生产操作。

## 验收

```powershell
python -B -m pytest backend/tests/api/test_admin_invoice_api.py -q --no-cov -p no:cacheprovider
python -B -m ruff check backend/app/api/admin/invoices.py backend/app/service/invoice/admin.py backend/app/repository/invoice_repo.py backend/tests/api/test_admin_invoice_api.py
python -B -m ruff format --check backend/app/api/admin/invoices.py backend/app/service/invoice/admin.py backend/app/repository/invoice_repo.py backend/tests/api/test_admin_invoice_api.py
python -B backend/scripts/check_project_development_register.py
python -B backend/scripts/check_mistake_ledger.py
git diff --check
```

验收结果：发票专用测试 7 项全部通过，Ruff 检查与格式检查通过。后续具备授权条件时，完成 P2 手册 E1-E4 真人/后台联动验收；在此之前 `T-P1-5-INVOICE` 保持阻塞，不能将其标记为完成。
