# P1-5 后续任务指令：发票状态与必填校验修复

> owner: AI 员工（实现）＋项目负责人（业务口径确认）
> status: pending
> status_label: 待处理（pending）
> task_id: T-P1-5-INVOICE-FIX
> as_of_commit: b4f25db
> version: 0.133.0-p2trial.3
> branch: main
> allowed_paths: `backend/app/api/admin/invoices.py`、`backend/app/service/invoice/**`、`backend/app/repository/invoice_repo.py`、`backend/tests/api/test_admin_invoice_api.py`、本任务文件、`PROJECT-STATE.md`、`LOGBOOK.md`、`ERRORS.md`
> forbidden_paths: `miniapp/**`、生产环境、真实支付、业务数据、旧仓 `D:\Project\YunxiBakeMiniApp`
> source_of_truth: PROJECT-STATE.md

## 目标

修复 P1-5 专用测试已确认的两个实现缺口，完成后台发票登记的最小状态闭环：

1. `companyTitle`、`taxNo`、`email` 缺失或去空格后为空时，接口返回 400 或 422，不写入空字段记录。
2. 已为 `issued` 的记录再次标记，以及其他非 `applied` 状态标记，接口返回 409。
3. 保持现有登记、列表、首次标记已开票行为不变。

## 实施边界

- 校验可放在请求模型或服务层，但错误响应必须稳定、可测试，且不能依赖数据库 `DEFAULT ''` 充当业务校验。
- 状态流转以服务层为业务守卫；仓储层必须能区分“记录不存在”和“状态不允许更新”。
- 不新增依赖，不改 miniapp，不执行真实支付或生产操作。
- 修改前先取得项目负责人对必填字段和 409 状态码的确认；未确认前只允许补测试和文档。

## 验收

```powershell
python -B -m pytest backend/tests/api/test_admin_invoice_api.py -q --no-cov --basetemp=D:/Project/.tmp-20260830-invoice-fix/pytest-base
python -B backend/scripts/check_project_development_register.py
python -B backend/scripts/check_mistake_ledger.py
git diff --check
```

验收目标：发票专用测试全部通过（不得保留严格 `xfail`），并在后续具备授权条件时完成 P2 手册 E1-E4 真人/后台联动验收。
