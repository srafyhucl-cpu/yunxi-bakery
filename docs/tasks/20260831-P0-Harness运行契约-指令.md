# P0 任务指令：统一运行 manifest 与 episode 回放

> owner: AI 员工
> status: completed
> status_label: 已完成（completed）
> task_id: T-HARNESS-RUN-MANIFEST
> as_of_commit: b4f25db
> version: 0.133.0-p2trial.3
> branch: main
> allowed_paths: `docs/harness-engineering/core/run-manifest.schema.json`、`docs/harness-engineering/core/traceability-model.md`、`docs/harness-engineering/core/agent-handoff-template.md`、`backend/scripts/harness_run_manifest.py`、`backend/scripts/check_harness_run_manifest.py`、`backend/tests/scripts/test_harness_run_manifest.py`、`backend/reports/harness/**`、本任务文件
> forbidden_paths: `backend/app/**`、`miniapp/**`、`backend/data/**`、生产环境、真实支付、客户原文、旧仓 `D:\Project\YunxiBakeMiniApp`
> source_of_truth: PROJECT-STATE.md

## 本轮目标

为中大型 Agent 运行建立不可覆盖的 JSON manifest 和中文 Markdown episode 摘要，记录 `run_id`、策略哈希、验证、证据、失败分类、成本、延迟、人工介入和回放状态。

## 执行步骤

1. 使用 `python -B backend/scripts/harness_run_manifest.py` 生成运行 manifest 和摘要。
2. 使用 `python -B backend/scripts/check_harness_run_manifest.py --summary` 批量校验报告。
3. 完成态必须同时满足结果正确、策略合规、证据完整、可回放四项断言。

## 验收

- schema、生成器和检查器定向测试通过。
- 报告文件拒绝覆盖旧文件，重试使用新的 `run_id`。
- 运行摘要不包含密钥、客户原文或未经批准的生产数据。

## 本轮追溯

- trace_id: `20260831-p0-execution`
- run_id: `20260831-p0-execution-r1`
- replayable: 是；失败分类：无；人工介入：无。
- completed_at: 2026-08-31
- evidence: `backend/reports/harness/20260831-p0-execution.run.json`；manifest 检查 total=1、failed=0
- result: 已完成（completed）；中大型运行具备不可覆盖的 JSON manifest 和中文 episode。
