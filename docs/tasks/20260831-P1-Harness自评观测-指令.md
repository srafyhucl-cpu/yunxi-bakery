# P1 任务指令：Harness 自评、趋势基线、运行观测与恢复能力

> owner: AI 员工
> status: completed
> status_label: 已完成（completed）
> task_id: T-HARNESS-EVAL-REGRESSION
> as_of_commit: b4f25db
> version: 0.133.0-p2trial.3
> branch: main
> allowed_paths: `docs/harness-engineering/evals/`、`backend/scripts/harness_eval_regression.py`、`backend/scripts/observe_harness_runs.py`、`backend/tests/scripts/test_harness_eval_regression.py`、`backend/tests/scripts/test_observe_harness_runs.py`、`backend/reports/harness/`
> forbidden_paths: `backend/app/**`、`backend/data/**`、`backend/reports/production/**`、`.env*`、`.git/**`、生产环境、真实支付、客户原文
> source_of_truth: PROJECT-STATE.md

## 本轮目标

在 P0 运行契约基础上建立最小持续质量闭环：固定自评数据集、评估器版本、通过阈值、趋势差异、运行观测指标和恢复点覆盖率。

## 执行命令

```powershell
python -B backend/scripts/harness_eval_regression.py --summary --output backend/reports/harness/harness-eval-baseline-20260831.json
python -B backend/scripts/observe_harness_runs.py --summary --output backend/reports/harness/harness-observation-20260831.json
```

## 完成标准

- 自评集至少覆盖方向一致性、权限边界、运行契约、证据完整性、中文治理、门禁一致性、恢复能力和策略稳定性。
- 输出 dataset_version、evaluator_version、threshold、失败分类和结果比例。
- 运行观测输出回放、恢复点、父子运行关联、失败分类、延迟和工具调用指标。
- P1 观测失败只阻断 P1 报告，不改变 P0 高风险执行门禁；真实客户数据和生产运行仍保持禁止。
