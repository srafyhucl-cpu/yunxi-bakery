# P1-5 任务指令：发票承接验收与补测

> owner: AI 员工（测试与验收）
> status: blocked
> status_label: 已阻塞（blocked）
> task_id: T-P1-5-INVOICE
> as_of_commit: b4f25db
> version: 0.133.0-p2trial.3
> branch: main
> allowed_paths: `backend/tests/**`、本任务文件、`PROJECT-STATE.md`（owner 收口）、`LOGBOOK.md`（owner 收口）
> forbidden_paths: `backend/app/**` 业务实现、`miniapp/**`、生产环境、旧仓 `D:\Project\YunxiBakeMiniApp`
> source_of_truth: PROJECT-STATE.md

## 本轮执行记录（2026-08-30）

- 新增 `backend/tests/api/test_admin_invoice_api.py`。
- 定向结果：3 项核心流程通过，4 项严格预期失败（重复 `issued` 1 项；缺少企业抬头、税号、邮箱 3 项）。
- 已确认业务缺口：重复标记已开票当前仍返回 200；请求模型将缺字段归一为空字符串，未返回 400/422。
- E1-E4 真人/后台联动未执行，仍受 P2 负责人批准、测试号和体验版条件约束。
- 按任务约束未修改 `backend/app/**`；任务转为已阻塞（blocked），待业务实现修复后移除严格预期失败并完成 E1-E4。

> **用法**：复制下方代码块内的全部内容，粘贴到新的 AI 会话中执行。
> **编写时间**：2026-08-29 ｜ **预计耗时**：半天
> **⚠️ 任务性质已修正**：这项不是「待实现」，代码已在 `e2639d4` 落地。
> 真正缺的是**专用测试**与**端到端验收**。详见下方背景。

---

```
你正在处理「芸熙烘焙智能经营平台」项目。

仓库路径：D:\Project\YunxiBakery（Monorepo，含 backend/ 与 miniapp/）
当前版本：0.133.0-p2trial.3
最新提交：b4f25db

## 任务

trace_id: 20260829-invoice-acceptance
parent_trace_id: 20260825-p1-invoice-mvp-plan

为已落地的发票承接功能补齐专用测试，并完成端到端验收。

## 背景与重要纠正

PROJECT-STATE.md 当前口径为「发票承接：代码已落地；专用测试与 E2E 验收待完成」。
此前「规划已定稿，P2 试运行前执行」的状态已过时；代码在 commit `e2639d4`（2026-08-25）
「P2 试运行准备完成——下架商品一致化 + 发票承接落地」中已实现。

已核实的实现清单（均已确认存在）：

- backend/app/api/admin/invoices.py —— 3 个端点：
  POST ""、GET ""、POST "/{invoice_id}/mark-issued"
- backend/app/migrations/v027_invoice_requests.sql —— invoice_requests 表
- backend/app/repository/invoice_repo.py —— 仓储层
- backend/app/service/invoice/ —— 服务域
- backend/web/admin/src/pages/invoices —— admin 前端页面
- backend/web/admin/dist/assets/InvoicesPage-*.js —— dist 已构建

与方案 docs/specs/2026-08-25-invoice-mvp-plan.md 第三节的表结构与 API 逐项对齐。

**唯一缺口：`backend/tests/` 下没有任何 invoice 专用测试文件。**
所以本任务是「补测试 + 验收」，不是「实现功能」。

## 你要做的事

### 1. 补专用测试

覆盖以下路径，测试文件放 backend/tests/ 下对应目录：

- 登记：POST /api/v1/admin/invoices 成功落库，status 默认为 applied
- 列表：GET /api/v1/admin/invoices 返回登记记录
- 标记已开：POST /api/v1/admin/invoices/{id}/mark-issued，status 变 issued
- 状态流转守卫：非法流转（如已 issued 再标记、cancelled 后标记）应被拒绝
- 校验：company_title / tax_no / email 为 NOT NULL，缺失应报错

### 2. 端到端验收（对照方案第四节 + P2 手册 E 项）

方案第四节的两条验收：

1. 顾客侧问「能开发票吗」→ AI 命中 #9566 话术
   （增值税电子普通发票；需企业抬头、税号、接收邮箱；交易完成后 1-3 个工作日开出）
2. 复杂情况转人工 → 人工在 admin 登记记录

P2 手册 E 项 E1–E4：

| 步骤 | 操作 | 预期 |
|------|------|------|
| E1 | 客服页发"我需要开发票"（附抬头/税号/邮箱） | AI 回开票口径 + 触发转人工 |
| E2 | 店员侧确认收到人工会话提示 | 会话标记转人工成功 |
| E3 | 负责人登录 admin 后台 → 发票管理 → 登记该请求 | 登记成功，状态 applied |
| E4 | 后台点「标记已开」 | 状态变 issued，可填备注 |

### 3. 同步 PROJECT-STATE.md

请围绕专用测试和 E2E 验收更新状态并注明结论。**这份文档是项目唯一状态入口，不允许长期失真。**

## 约束（违反即返工）

- 本任务**不改业务代码**，只加测试 + 验 + 更新状态文档；
  若测试暴露真实缺陷，先报告，不要顺手改实现
- 不引入新依赖
- 代码注释统一中文
- 分层红线：api → service → repository → models，禁止向上穿透
- SQL 必须 `?` 参数化，禁止 f-string 拼接
- **不部署、不执行真实支付、不开放真实用户访问**
- 不碰旧仓 D:\Project\YunxiBakeMiniApp（已冻结）

## 环境注意（本项目历史坑）

- `python` 必须指向 `backend/.venv`，否则 pre-commit 会整片报
  `ModuleNotFoundError: No module named 'pydantic'`
- 提交前 `unset PYTHONPATH`，否则注入的 shim 会让 `test_red_line_rules.py`
  的 unlink 全部 fail-closed
- pytest 一律带仓库外 basetemp：`--basetemp=D:/Project/.tmp-<date>/pytest-base`
- 禁止用 `npm run xxx | tail` 判断退出码，管道会吞掉退出码
- push 用 `git -c credential.helper='!gh auth git-credential' push origin main`

## 提交收口（按 docs/AGENTS/commit-workflow.md 9 步）

- 本克隆只有 `origin` 一个远端，`git push server master` **无对应远端，不要执行**
- VERSION 变更后必须同步根目录与 backend/ 两份 `项目进度与配置清单.md` 第 3 行版本号
- LOGBOOK.md 在**文件顶部**追加条目，含 changed_files / implementation /
  verification / residual_risks
- push 后必须 `git ls-remote origin main` 回读核对 SHA

## 汇报格式

1. 新增测试文件路径 + 通过数量 + 实际 EXIT 值（贴命令输出，不要只写"通过"）
2. 端到端验收 E1–E4 逐条结果（已验证 / 未验证 / 阻塞原因）
3. PROJECT-STATE.md 第 89 行改成什么
4. commit SHA、新版本号、两份进度清单表头是否同步
5. 遗留风险（没有就写"无"，不要编造）
```

---

## 附：编写本指令时的核实记录

| 核实项 | 结果 | 核实方式 |
|--------|------|---------|
| admin API 端点 | 3 个：POST/GET/mark-issued | `grep @router. invoices.py` |
| 表迁移 | v027_invoice_requests.sql，字段与方案一致 | 读文件 |
| 仓储层 | app/repository/invoice_repo.py | 列目录 |
| 服务域 | app/service/invoice/ | 列目录 |
| admin 前端 | src/pages/invoices + dist 已构建 | 列目录 |
| **专用测试** | **无** | `find backend/tests -iname "*invoice*"` 无结果 |
| PROJECT-STATE L89 状态 | 仍写"待执行"，**已过时** | 读文件 |

> 结论：这项的真实剩余工作量是「补测试 + 验收」，不是原状态文档所说的「半天实现」。
