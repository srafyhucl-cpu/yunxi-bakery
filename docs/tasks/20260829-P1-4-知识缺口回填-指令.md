# P1-4 任务指令：知识缺口回填

> owner: AI 员工（内容准备与入库验证）
> status: pending
> task_id: T-P1-4-KNOWLEDGE
> as_of_commit: 77f9346
> version: 0.133.0-p2trial.3
> branch: main
> allowed_paths: `docs/specs/faq-template-draft.md`、`backend/data/**`（经负责人批准后写入）、本任务文件、`LOGBOOK.md`（owner 收口）
> forbidden_paths: `backend/app/**`、`miniapp/**`、检索算法与向量路径、旧仓 `D:\Project\YunxiBakeMiniApp`、生产环境
> source_of_truth: PROJECT-STATE.md

> **用法**：复制下方代码块内的全部内容，粘贴到新的 AI 会话中执行。
> **编写时间**：2026-08-29 ｜ **预计耗时**：1–2 小时（含确认等待）
> **前置**：无技术阻塞（转人工兜底已验证可用），属 P2 试运行前的内容补强。

---

```
你正在处理「芸熙烘焙智能经营平台」项目。

仓库路径：D:\Project\YunxiBakery（Monorepo，含 backend/ 与 miniapp/）
当前版本：0.133.0-p2trial.3
最新提交：77f9346

## 任务

trace_id: 20260829-knowledge-gap-backfill
parent_trace_id: 20260825-p1-wrap

回填 P1 阶段识别出的 5 条 production 知识缺口，并补充 FAQ 内容。

## 背景

PROJECT-STATE.md 第 54 行记载：「5 条 production 缺口（knowledge_gaps）+ FAQ 补充
均不阻塞——转人工兜底已验证，回填排入 P2 试运行前」。

性质属**内容补强**，不是缺陷修复：缺口已由转人工链路兜底，不会导致线上错误。

## ⚠️ 关键前提：仓库内没有现成的缺口清单

我已全仓检索，`knowledge_gaps` 只在以下位置出现，**没有任何一份文档列出那 5 条的具体内容**：

- backend/app/migrations/v004_agent_foundation_tables.sql 第 40 行 —— 表定义
- PROJECT-STATE.md 第 54 行、第 88 行 —— 只写了「5 条」这个数量

**因此第一步必须是枚举，不是回填。** 不要凭猜测编造 5 条内容。

### 第一步：枚举并确认

1. 从 `knowledge_gaps` 表读出全部 status 未结的条目，确认数量是否为 5；
   若数量不符，如实报告差异，不要硬凑成 5 条。
2. 输出一份清单（每条含：用户问法 / 当前缺失内容 / 建议补的口径 / 建议归属 FAQ 或知识条目），
   **先交给项目负责人确认口径，再动写入。**
3. 店家口径类内容（价格、营业时间、配送范围、售后规则等）一律以店家答复为准，
   禁止自行编造业务事实。

## 第二步：回填

按确认后的清单写入知识库，并补 FAQ 内容。
可直接复用的现成材料：`docs/specs/faq-template-draft.md`（10 条 FAQ 模板草案）。

## 验收

1. `python backend/scripts/check_knowledge.py` 通过（脚本在 backend/scripts/）
2. 逐条做 BM25 检索命中验证：用清单里的用户问法检索，确认能命中新写入的条目
   ——**写入不等于可检索，必须验证命中**
3. 迁移后的 24 条人工沉淀知识检索无退化

## 约束（违反即返工）

- 只做内容写入与验证，不改检索算法、不改 BM25 配置、不动向量路径（已战略禁用）
- 代码注释统一中文
- 不碰旧仓 D:\Project\YunxiBakeMiniApp（已冻结）
- **不部署、不执行真实支付、不开放真实用户访问**
  （截至 2027-05-31 含，小程序仅限开发、调试和测试）
- 若枚举发现缺口数量或内容与「5 条」不符，**如实报告，不要为了对齐而调整口径**

## 环境注意（本项目历史坑）

- `python` 必须指向 `backend/.venv`，否则 pre-commit 会整片报
  `ModuleNotFoundError: No module named 'pydantic'`
- 提交前 `unset PYTHONPATH`，否则注入的 shim 会让 `test_red_line_rules.py`
  的 unlink 全部 fail-closed
- pytest 一律带仓库外 basetemp：`--basetemp=D:/Project/.tmp-<date>/pytest-base`
- 禁止用 `npm run xxx | tail` 判断退出码，管道会吞掉退出码
- push 用 `git -c credential.helper='!gh auth git-credential' push origin main`
  （本环境 GCM 需要 TTY）

## 提交收口（按 docs/AGENTS/commit-workflow.md 9 步）

- 本克隆只有 `origin` 一个远端，文档里的 `git push server master` **无对应远端，不要执行**
- VERSION 变更后必须同步根目录与 backend/ 两份 `项目进度与配置清单.md` 第 3 行版本号
- LOGBOOK.md 在**文件顶部**追加条目，含 changed_files / implementation /
  verification / residual_risks
- push 后必须 `git ls-remote origin main` 回读核对 SHA

## 汇报格式

1. 枚举出的缺口清单（含确认状态：项目负责人已确认 / 待确认）
2. 每条缺口的回填内容与 BM25 命中验证结果（贴命令输出）
3. check_knowledge.py 的 EXIT 值
4. commit SHA、新版本号、两份进度清单表头是否同步
5. 遗留风险（没有就写"无"，不要编造）
```

---

## 附：编写本指令时的核实记录

| 核实项 | 结果 | 核实方式 |
|--------|------|---------|
| `knowledge_gaps` 表存在 | v004_agent_foundation_tables.sql 第 40 行 | 读迁移文件 |
| 仓库内是否有 5 条清单 | **无**，仅 PROJECT-STATE.md L54/L88 提到数量 | 全仓检索 `knowledge_gaps` / `缺口` |
| 检查脚本 | `backend/scripts/check_knowledge.py` | 列目录 |
| FAQ 现成模板 | `docs/specs/faq-template-draft.md`，10 条 | 读文件 |
