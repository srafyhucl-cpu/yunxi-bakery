# P0 任务指令：Harness 临时与可重建产物递归清理规则收口

> owner: AI 员工＋项目负责人
> status: completed
> status_label: 已完成（completed）
> task_id: T-HARNESS-CLEANUP-POLICY
> as_of_commit: b4f25db
> version: 0.133.0-p2trial.3
> branch: main
> allowed_paths: `AGENTS.md`、`miniapp/AGENTS.md`、`docs/AGENTS/`、`docs/harness-engineering/`、`backend/.agents/skills/yunxi-harness-engineering/SKILL.md`、`scripts/cleanup-local-artifacts.ps1`、`backend/tests/scripts/test_cleanup_local_artifacts.py`、`backend/tests/scripts/test_harness_policy.py`、本任务文件、`PROJECT-STATE.md`、`LOGBOOK.md`
> forbidden_paths: `backend/app/**`、`backend/data/**`、`backend/reports/production/**`、`.env*`、`.git/**`、生产环境、真实支付、客户数据、任意路径递归删除、旧仓 `D:\Project\YunxiBakeMiniApp`
> source_of_truth: PROJECT-STATE.md

## 本轮目标

允许及时递归批量清理明确白名单内的临时与可重建产物，同时继续阻断未知路径、业务数据、生产目录、有效报告和其他 Agent 有效工件的递归删除。

## 执行规则

1. 清理入口统一使用 `scripts/cleanup-local-artifacts.ps1`。
2. 先运行预览模式，核对目标路径、文件数量和保护边界，再使用 `-Execute` 执行。
3. 自定义目录仅允许工作区内或 `D:\Temp` 下、使用 `.tmp-`/`pytest-` 前缀的临时目录。
4. `backend/data/`、报告目录、`.env*`、`.git/`、生产目录和未知目录继续保持阻断。
5. 清理后记录范围、数量、失败项和剩余目录状态；不覆盖既有报告。

## 本轮追溯

- trace_id: `20260831-cleanup-policy`
- run_id: `20260831-045839-140531c67c4d`
- replayable: 是；失败分类：无；人工介入：规则变更由项目负责人明确提出，执行仍需 `-Execute`。
- completed_at: 2026-08-31
- evidence: `backend/reports/harness/20260831-cleanup-policy-final-v2.run.json`；策略哈希以该 manifest 的 `tool_policy_hash` 为准。
- result: 已完成（completed）；白名单临时/可重建产物可在预览和授权后递归批量清理，任意路径递归删除仍被策略阻断。
