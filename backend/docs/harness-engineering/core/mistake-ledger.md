# 错误账本兼容入口

本文件是旧仓文档镜像中的兼容占位，不再承载正式错误条目。

唯一正式错误账本是 Monorepo 根目录 [`ERRORS.md`](../../../../ERRORS.md)。请直接编辑该文件，并运行：

```powershell
python -B backend/scripts/check_mistake_ledger.py
```

旧仓镜像仅用于历史追溯；不得在本文件新增或复制条目。
